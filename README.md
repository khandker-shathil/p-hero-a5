1️⃣ What is the difference between var, let, and const?
<table> Ans:
<th>Var</th>
<th>Let</th>
<th>Const</th>
<tr><td>Globally scoped, compormises security. Can be redeclared. Can be reassigned. </td>
<td>Block scoped. Can't be redclared. Can be reassigned. </td>
<td>Block scoped. Can't be redeclared. Can't be reassigned. </td></tr>
</table><br>
<hr>
2️⃣ What is the spread operator (...)?<br>
Ans: Spread operator allows arrays, strings to be expanded where zero or more arguments are expected. In object, the spread operator makes a key-value object.<br> 
<hr>
3️⃣ What is the difference between map(), filter(), and forEach()? <br>
Ans: <table>
<th>map()</th>
<th>filter()</th>
<th>forEach()</th>
<tr><td> Non Mutating method. Creates a new copy of the exsisting array based on the condition provided. </td>
<td> Returns an array if a given condition is passed. Good for searching and finding elements inside an existing array. </td>
<td> Array method that runs on each element in an Array. </td></tr>
</table>
<br>
<hr>
4️⃣ What is an arrow function?<br>
Ans: Another way of creating methods or function in the lastes ES6 version of JavaScript with some bindings limitation. 
<br>
<hr>
5️⃣ What are template literals?<br>
Ans: A modern way of dealing with string in ES6 `` Enclosed by backticks. Can add any JavaScript variables ${} inside a string (String  Interpolation). Can do Multiple line of strings. 

